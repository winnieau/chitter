
<div class="peep_page">
  <div class="peep_list">

    <h1>Peeps</h1>
    <ul id="peeps">
      <% @peeps.each do |peep| %>
        <div id="each_peep">
          <%= peep.content %>
        </div>
      <% end %>
    </ul>
  </div>
</div>
